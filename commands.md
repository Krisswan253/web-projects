## git commands to run every time

1. check the files have been added

```bash
git status
```

2. add all the new files

```sh
git add .
```

3. stage the files with a message to explain what the changed files are doing

```sh
git commit -m 'this is a message that explains what the change did'
```

4. push the changes to the repo
```sh
git push
```